# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache
/.lighthouseci

# env — never commit real secrets (see .env.example for the template)
.env
.env.local
.env.*.local

# vercel
.vercel

# Claude Code skills — vendored skill bundles are installed locally per-dev, not
# committed (heavy). Personal settings + local tool state stay out of the repo.
.claude/skills/
.claude/settings.local.json
.impeccable/

# misc
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.tsbuildinfo
